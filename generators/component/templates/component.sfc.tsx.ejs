import React, { FC } from 'react';

interface <%= myComp %>Props {
    myProp: any;
}

// tslint:disable-next-line:variable-name
export const <%= myComp %>: FC<<%= myComp %>Props> = ({ myProp }) => (
    <div>{myProp}</div>
);
